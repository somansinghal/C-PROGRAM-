#include <stdio.h>
int main() {
    int a,b,c;
    printf("Enter three integer: ");
    scanf("%d %d %d",&a ,&b, &c);
    
    if (a >= b && a >= c)
        printf("Highest : %d\n",a);
    else if (b >= a && b >= c)
        printf("Highest : %d\n",b);
    else
        printf("Highest : %d\n",c);
    
    return 0;
}
