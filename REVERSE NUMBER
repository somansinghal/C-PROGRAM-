#include <stdio.h>
int main() {
    int num, rever = 0;
    printf("Enter a number: ");
    scanf("%d",&num);
    
    while (num != 0) {
        rever = rever * 10 + num % 10;
        num /= 10 ;
    }
    printf("Reversed number : %d\n", rever);
    return 0;
}
