#include <stdio.h>
int main() {
    int num;
    printf("Enter number which to print in matrix : ");
    scanf("%d" , &num);
    for (int i = 0; i < 5; i++) {
        for (int j = 0; j < 5; j++)
            printf("%d ", num);
        printf("\n");
    
    }
    
    return 0;
}
