#include <stdio.h>
int main() {
    int n, i;
    printf("Enter a number (n): ");
    scanf("%d", &n);
    printf("Odd numbers up to %d are: \n", n);
    for (i = 1; i <= n; i++) {
        if (i % 2 != 0) {
            printf("%d ", i);
        }
    }
    printf("\n");
    return 0;
}
